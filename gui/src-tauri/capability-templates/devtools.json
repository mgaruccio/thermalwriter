{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Dev capabilities for the thermalwriter config GUI (includes MCP bridge).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "mcp-bridge:default"
  ]
}
